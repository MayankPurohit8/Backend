<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-screen bg-zinc-900">
        <ul class="text-gray-500 text-2xl flex gap-20 p-5 order-solid border-2 border-gray-600 rounded-lg ">
            <li class="hover:text-3xl"><a href="/">HOME</a></li>
            <li class="hover:text-3xl"><a href="/read">DISPLAY</a></li>
        
        </ul>
        
        
        <%if(users.length>0){%>
            <div class="p-10 text-white  text-4xl">ALL USER</div>
            <div class="flex  gap-2">
        <%users.forEach(function(user){%>
            <div class=" text-center text-white font-bold p-2 mx-10 w-64 h-80 bg-zinc-500 rounded-lg flex justify-center flex-col">
                <img src="<%= user.url %>" alt="" srcset="" class="w-60 h-60 rounded-lg">
                <div class=""><%= user.name%></div>
                <div class=""><%= user.email%></div>
                <ul class="flex justify-around">
                    <li><a class="text-blue-800" href="/edit/<%= user._id%>">Edit</a></li>
                    <li><a class="text-red-900 underline" href="/delete/<%= user._id %>">Delete</a></li>
                </ul>
            </div>
        <%})%>
        </div></div>
        <%}else{%>
            <div class="text-5xl text-gray-500 p-20">NO USERS YET</div>
        <%}%>
            
    </div>
</body>
</html>