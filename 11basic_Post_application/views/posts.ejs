<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="bg-zinc-900 h-max flex flex-col gap-10">
        <div class="w-full  p-10">
            <a href="/logout" class="p-2 rounded-lg bg-red-700 text-white block w-min ">Logout</a>
            <div class="text-white text-2xl py-5  ">Hello <%=user.username%>,</div>
            <div class="text-white text-2xl py-5  ">Posts: </div>
        </div>
        
        <div class=" p-10 flex flex-col gap-5 h-screen ">
            
            <% allposts.reverse().forEach(function(post){%>
                <%if(post.use!=user._id){%>
                <div class="posts w-1/3 flex flex-col gap-5 ">
                    <div class=" p-5 rounded-lg  bg-zinc-800 ">
                        <div class="text-blue-700">@<%=post.user.username%>
                        </div>
                        <div class="text-white">
                            <%= post.content%>
                        </div>
                        <div class="btns py-3 flex justify-between w-1/3">
                            <%if(post.likes.includes(user._id)){%>
                                <a href="/like/<%=post._id%>" class="bg-red-900 text-white p-2 rounded-xl w-20">
                                    <%=post.likes.length%> UnLike
                                </a>
                                <%}else{%>
                                    <a  href="/like/<%=post._id%>" class="bg-red-900 text-white p-2 rounded-xl w-20">
                                        <%=post.likes.length%> Like
                                    </a>
                                    
                                    <%}%>
                                        
                        </div>
                    </div>
                </div>
                <%}})%>

        </div>

    </div>
</body>

</html>